<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="onNavigatingTo">
    <ActionBar title="{{ isUpdate ? 'Editar Elemento' : 'Nuevo Elemento' }}" class="action-bar" />
    
    <StackLayout class="form-container">
        <Label text="Título" class="input-label" />
        <TextField text="{{ title }}" hint="Ingresa el título" class="input-field" />
        
        <Label text="Subtítulo" class="input-label" />
        <TextField text="{{ subtitle }}" hint="Ingresa el subtítulo" class="input-field" />
        
        <Label text="Estado Activo" class="input-label" />
        <Switch checked="{{ isActive }}" class="switch-field" />
        
        <Button text="Guardar" tap="onSave" class="btn-primary" />
    </StackLayout>
</Page>